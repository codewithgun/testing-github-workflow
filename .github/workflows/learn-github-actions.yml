name: learn-github-actions

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  some_random_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
      - run: echo "Hello world"
        shell: bash
